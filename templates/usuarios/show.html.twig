{% extends 'base.html.twig' %} {% block title %}Usuarios{% endblock %} {% block body %}
<div class="container">
    {% block navbar %} {% include ('navbar.html.twig') %} {% endblock navbar %}
    <div class="py-3">
        <div class="row">
            <div class="col-md-10">
                <h5 class="ml-5 text-info">Dados Pessoais</h5>
            </div>
        </div>
    </div>
    <div class="py-1 ml-5 col-md-10 border border-warning">
        <div class="row">
            <div class="col-md-10">
                <br>
                <div class="table-responsive-md">
                    <table class="table table-hover table-sm">
                        <tbody>
                            <!--<tr>
                                <th>Id</th>
                                <td>{{ usuario.id }}</td>
                            </tr>-->
                            <tr>
                                <th>Nome</th>
                                <td>{{ usuario.nome }}</td>
                            </tr>
                            <tr>
                                <th>Apelido</th>
                                <td>{{ usuario.apelido }}</td>
                            </tr>
                            <tr>
                                <th>Email</th>
                                <td>{{ usuario.email }}</td>
                            </tr>
                            <tr>
                                <th>Login</th>
                                <td>{{ usuario.login }}</td>
                            </tr>
                            <!--<tr>
                                <th>Password</th>
                                <td>{#{{ usuario.password }}#}</td>
                            </tr>-->
                            <tr>
                                <th>Activo</th>
                                <td>{{ usuario.activo ? 'Yes' : 'No' }}</td>
                            </tr>
                            <tr>
                                <th>Registo</th>
                                <td>{{ usuario.registo ? usuario.registo|date('Y-m-d H:i:s') : '' }}</td>
                            </tr>
                            <tr>
                                <th>Registo_update</th>
                                <td>{{ usuario.registoUpdate ? usuario.registoUpdate|date('Y-m-d H:i:s') : '' }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="ml-3 col-md-10">
                <div class="pt-2 text-left">
                    <a href="{{ path('usuarios_edit', {'id': usuario.id}) }}"><button class="btn btn-outline-info"><i class="fas fa-pencil-alt fa-1x"></i></button></a>
                </div>
                <div class="pb-2 text-right">
                    {{ include('usuarios/_delete_form.html.twig', {'button_label': 'delete'}) }}
                </div>
            </div>
        </div>
    </div>
    <div class="py-3">
        <div class="row">
            <div class="ml-5 col-md-10 text-center">
                <a href="{{ path('usuarios_index') }}"> <button class="btn btn-outline-info" title="Lista de usuários">Lista Usuários</button></a>
            </div>
        </div>
    </div>
    {% block footer %} {% include ('footer.html.twig') %} {% endblock footer %}
</div>

{% endblock %}